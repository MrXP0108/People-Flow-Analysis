# BEFORE YOUR pip install...
# 1. install Microsoft Visual C++ (follow the steps in https://blog.csdn.net/bluishglc/article/details/128889935)
# 2. run "pip install setuptools"
# 3. run "python -m pip install pip==21.1.1"

numpy>=1.10.1
filterpy
lap==0.4.0
scipy
argparse
ultralytics
opencv-python
imutils
pillow
matplotlib

arch==5.3.1
image_quality==1.2.7
lpips==0.1.4
einops==0.6.1

# run the following commands for GPU support
# $ pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu[version]
# $ pip install cupy-cuda[version]
